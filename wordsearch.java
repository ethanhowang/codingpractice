class Solution {
    public findneighbors(char[][], int row, int col) {
      int rows[]
    }
    public boolean exist(char[][] board, String word) {
        if  (word == "") {
          return true;
        }
        int m = board.length;
        int n = board[0].length;

        for (int i=0; i < m; i++) {
            for (int j=0; j < n; j++) {
                if (board[i][j] == word.charAt(0)) {

                }
            }
        }
        return false;
    }
}

Solution.exist()
